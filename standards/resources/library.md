# 资料库规范

## 目标

降低入库错误，保证网站可稳定同步。

## 链路

入库工具 -> `.resource.yml` -> `resources.index.json` -> 网站 `resources:sync`

## 规则

1. 优先使用入库工具，不手工复制文件。
2. 单文件大小控制在 100MB 内（工具已拦截）。
3. 标签避免手写引号，保持简洁一致。
4. 删除条目优先用工具执行，避免索引与元数据不一致。
