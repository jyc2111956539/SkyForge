# 技术文章最小规范

## 目标

允许短文快速发布，同时保证可检索与可维护。

## 必填 frontmatter

- `title`
- `createTime`
- `permalink`
- `tags`

## 推荐规则

1. 标题优先写“问题+对象”（例如：`ATMEGA328P 烧录失败排查`）。
2. 标签建议 2-5 个，使用固定词汇（如 `Arduino`、`STM32`、`Linux`、`驱动`、`调试`）。
3. 短文允许只有 3-10 行正文，不强制长模板。
4. 有结论就先发，后续可增量补图与细节。
